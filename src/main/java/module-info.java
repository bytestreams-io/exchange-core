/** Core interfaces and channels for request-response message exchange. */
module io.bytestreams.exchange.core {
  requires io.opentelemetry.api;
  requires org.slf4j;

  exports io.bytestreams.exchange.core;
}
